'use strict';
const fs=require('fs');
const path=require('path');
const root=path.resolve(__dirname,'..');
require('./patch-objects-570.js');
const ioFile=path.join(root,'io-package.json');
const io=JSON.parse(fs.readFileSync(ioFile,'utf8'));
io.version='0.5.71';
io.common=io.common||{};
io.common.version='0.5.71';
fs.writeFileSync(ioFile,JSON.stringify(io,null,2)+'\n');
console.log('[0.5.71] Phone-Temperaturkurve direkt aus vorhandener SVG in die Temperaturzeile kopiert; Gesamtgroesse unveraendert');
